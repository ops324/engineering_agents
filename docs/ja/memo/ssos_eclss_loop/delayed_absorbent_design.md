# 対処手段の設計・第2版 — 遅延のある消耗品吸収剤（**未実装・未監査**）

```yaml
起草:   2026-08-30
status: **設計。S1 は通した（EXP-039）。実装していない。監査も人間の判断も得ていない**
先行:   contingency_absorbent_design.md（第1版・**監査の S1 で落ちた**）
        repair_and_diagnosis_design.md（案4・EXP-019 で落ちた）
根拠:   EXP-017/018/019/027/030/037/038/039。すべて GPU 0秒
```

## 第1版が落ちた理由と、そこから変えた1点

```
第1版   有限在庫の吸収剤。判断は「いま使うか温存するか」。**未来を隠せば判断が生まれる**と考えた
落ちた  投入速度の上限も遅延も無いので、**1 kg が買う時間はいつ使っても同じ**。
       温存に得が無く「必要になった瞬間に必要な分だけ」が弱支配的。
       未来を全部見るオラクルが定数閾値に **48/48 で勝てない**（監査が実装せずに実測）
変えた1点 **投入から効くまでに遅延を入れる。** それだけ
```

**★ 判断を作るのは「取り返しのつかなさ」ではなく「先に決めねばならないこと」だった**（EXP-039）。

## S1 は通した — **設計文書を書く前に測ってある**

`scratchpad/audit/s1_commitment.py`（監査官の縮約模型を再利用・GPU 0秒）:

| 構造 | 勝率 | 差の中央値 | 乗員が動くセル |
|---|---|---|---|
| 構造なし（第1版） | **0/48** | 0.0 | 0/48 |
| 投入速度の上限 | 20/48 | 0.0（**最大 1 person-step ＝ 雑音**） | 0/48 |
| **遅延** | **38/48** | **36.5** | **9/48（乗員 3→4人 ＝ 12.5点）** |
| 最小投入単位 | 0/48 | 0.0 | 0/48 |

**窓 400 → 800 でも 38/48・中央値 36.5 で不変**（[EXP-037](../experiments/EXP-037-the-window-question-has-a-cheap-answer.md) の規則を通過）。

## ★ 遅延の値には出典が無い。だから「幅」で示す

**ROADMAP:311 は「定格・消耗速度は出典を当たってから決めること」と指示している。**
**この枝は実機の吸収剤の応答時間を確認していない。捏造しない。**
代わりに**結論が値に依存しないこと**を測った:

```
遅延(step)  勝率     差の中央値   乗員が動く      物理時間（1 step = 1200 秒）
   0       0/48      0.0       0/48       —
   1       1/48      0.0       0/48       20 分
   2      37/48     28.5       9/48       **40 分**  ← ここから効く
   3      38/48     36.5       9/48       1.0 時間
   5      39/48     38.5       8/48       **1.7 時間** ← 最良
  10      31/48     15.0       5/48       3.3 時間
  20      23/48      0.0       3/48       6.7 時間   ← 中央値ゼロ
```

**⇒ 効果は 40分〜3.3時間 の帯で成立する。** 実機の応答時間がこの帯に入るかは**出典を当ててから判断する**。
**入らなければ、この設計は落とす。**（＝ これも撤回条件である。下記 C6）

## 仕様

```
新しい状態   absorbent_stock_kg        有限・補給なし
            absorbent_pending[]        投入済み・未発効の量（遅延 D step のキュー）
新しい指令   use_absorbent {"amount": <kg>}
効果        投入した量は **D step 後に** co2_removal_per_kg × amount だけキャビン CO₂ を除去。
            在庫は**投入した時点で**減る（発効を待たない ＝ 外したら捨てになる）
故障        ARS 故障の継続時間と間隔は seed から決まる（run は決定的）が、**観測に出さない**
```

## 6条件の充足（[第1版の条件表](contingency_absorbent_design.md)を引き継ぐ）

```
A 費用       ✅ 在庫が減る。補給なし
B 別の使い道  ✅ 故障は複数回来る。早く使い切ると次で手当てが無い
C 正解が状況で変わる  ✅ **実測で満たした**（EXP-039・オラクルが定数に 38/48 で勝つ）
             ★ 第1版は「未来が観測から自明でない」に差し替えていた。**EXP-019 の原文に戻した**
D 使いすぎの罰 ✅ 在庫切れ後の故障で手当てゼロ
E 窓          ✅ 400 → 800 で不変
F 効果の大きさ ✅ **実測で満たした**（9/48 で乗員1人 ＝ 12.5点。状況の効果と同オーダー）
             ★ 第1版は「最大 50点」と書いたが実測上限は **35.91点**（CO₂ で失えるのは最大2人）
```

**第1版が落とした「不都合な条件」も入れる:**
```
・EXP-030 理由1  定数腕は提案と**同じ次元数**で張る → 定数側も (トリガ, 投入率) の2次元で張った
・EXP-030 理由2  **後知恵を除く** → 下の C5（ホールドアウト）
・EXP-026/035   **判断ゼロの入力を通す** → 下の C4
```

## 事前登録

### 主指標
```
person-steps（打ち切りに強い）を主、共通3軸（90点）を従。**生存者数は主指標にしない**
⚠ ただし共通3軸の 50/90 は残存＝12.5×乗員なので、**実質は乗員数に支配される**。
  第1版の監査が指摘したとおり（400 step の 12 run で A+B の全幅は 0.9634点）。
  **「1点以上」という判定は乗員が動かない限り達成不能** ⇒ 判定は person-steps で行う
```

### 判定規則
```
成立  ①先読みする政策が、**(トリガ, 投入率) の2次元で張った最良の定数**を person-steps で上回る
     ②その差の**中央値が 20 person-steps 以上**（EXP-039 の遅延2〜5 で 28.5〜38.5 だった水準）
     ③**窓を倍にして①②が生き残る**
     ④**ホールドアウトした seed で①②が生き残る**
不成立 どれか1つでも欠けたら落とす
```

### 撤回条件（**各条件に発火しうる入力が実在するか、実装前に確認する**）
```
C1 単一の定数が全状況で最適 → 落とす
   発火しうるか: ✅ 第1版で実際に発火した（0/48）。定数腕を並走させれば判定できる
C2 差が person-steps では出るが**乗員に一度も届かない** → 条件F を満たさない。落とす
   発火しうるか: ✅ 遅延20 では乗員が動くのは 3/48 まで落ちる。判定できる
C3 遅延を実機の出典に照らして 40分〜3.3時間 の帯**外**と分かった → 落とす
   発火しうるか: ✅ 出典を当てれば判定できる。**当てるまで実装しない**
C4 「常に使う」「常に温存」「固定量」の**判断ゼロの腕**が、先読み腕と同点以上 → 落とす
   発火しうるか: ✅ 第1版で実際にそうなった
C5 ホールドアウト seed で差が中央値 20 を割る → 落とす
   発火しうるか: ✅ seed を分割すれば判定できる
C6 本実装で公表値 82.2297 / 68.6661 が動く → **物理の副作用**。落とすか原因を特定するまで止める
   発火しうるか: ✅ 実装後 1 run 0.4秒で判定できる
```

### 予測（実装前に書く）
```
P1  本実装でも先読み腕が定数を上回る。ただし**縮約模型より差は小さい**
    （水・O₂ の結合と採点式が入るので、CO₂ 以外の制約が効く）
P2  ホールドアウトで差は縮むが、中央値 20 は維持する
P3  ★ **私はこの P1/P2 を 6回外している。** 同じ形の予測が5回続けて外れた記録がある
```

## ★ 自己申告する弱点

```
1  **オラクルは真の故障日程を見ている。** 実際の腕は推定しなければならない。
   S1 が示したのは「**先読みに価値がある**」であって「**LLM が取れる**」ではない。
   ⇒ 本測定では「推定できる腕」と「オラクル」の**両方**を並べること
2  **遅延 3 step も 5 step も著者が選んだ。** 帯で示したが、**帯そのものも著者の模型が決めている**
3  **縮約模型は CO₂ だけ。** 水・O₂ の結合が入ると結論が変わりうる
4  **故障の頻度・長さの分布を著者が選ぶ。** 難易度を著者が作れてしまう（第1版の弱点4 と同根）
5  **「判断が生まれるように作った」ことは変わらない。** 作れば生まれるのは当たり前で、
   **面白いかどうかは別**（EXP-025 第1版が落ちた型）。
   ⇒ 主張できるのは「この環境で判断を置ける最小の構造が1つ特定できた」までである
6  **実装は物理の追加＝世代が変わる（v6）。** 公表値が動かないことは C6 で確かめるが、
   **LLM 腕を回した過去の run とは比較できなくなる**
```

## 次の順序

```
S6 独立監査 → S8 人間の判断 → **出典を当てる（C3）** → 最小実装 → C1〜C6 の確認 → 本測定
```
