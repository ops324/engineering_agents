# EXP-040 — 識別子の照合は、いちばん要る場面で鳴るか【事前登録】

```yaml
experiment_id: EXP-040
date:          2026-08-30
branch:        experiment/eclss-evaluation-layer
方法:          EXP-035 の凍結入力に、**第4の腕（識別子の共有）**を同じ規則で当てる。決定的
費用:          **GPU 0秒・LLM 0本**（約15秒）
status:        **pre-registered（測定前。この文書のコミット時刻が「後出しでない」証拠）**
先行:          EXP-035（S2 ゲートの測定と監査）
```

## 経緯 — 「母集団を作る」は S1 で落ちた。その途中で腕が1つ出てきた

前回の次手は **A「S2 の母集団を作る（まだ誰も気づいていない再発見を掘る）」**だった。
**設計文書を書く前に成立するかを確かめ、成立しないと分かった**（EXP-026/027 と同じ規律）。
測定は `~/ea-runs/2026-08-30-s2-population/`（read-only・GPU 0秒）。

```
① 腕と独立な生成子が、記録のほとんどに触れない
   記録から主張を 2565件 抽出（birth はコミット履歴1パスで 2565/2565 解決）
   主題の識別子（snake/dotted）を持つ    193件 (7.5%)   ← 構造的生成子が届く範囲
   引用の識別子（EXP-NNN 等）を持つ      185件 (7.2%)   ← 先行を名指し＝再発見ではない印
   **どちらも持たない**                2251件 (87.8%)  ← **構造では触れない**

② 無作為抽出は、必要な標本数が答えに依存する
   既知の再発見 3件 / 2026-08-29 に生まれた主張 1578件 ⇒ **検出された基準率 0.19%**
   200件を網羅探索して期待正例 0.38。**5件の正例を期待するには約 2600件**
   ⚠ 0.19% は**検出された**率であって真の率の**下限**である。
     つまり必要な標本数は「未検出がどれだけあるか」＝ A が測りたかった当の量に依存する。
     **設計の実行可能性が、その設計の答えに依存している**

③ そして語彙的な生成子を使えば、生成子＝測る対象の腕になる
   → 母集団は腕2 が見つけられるものに偏り、腕2 の捕捉率は**上限**になる。
     EXP-035 が「正例が『気づけた件』に偏る」と書いたのと同じ型。**証拠が1ビットも出ない**

⇒ **A は「掘れば出る」形では成立しない。** ただし ① の途中で、次のことが分かった
```

**★ 構造的生成子は、EXP-035 が取り逃した Q1 に届く**（`check_generator.py`・実測）:

```
       クエリと正解チャンクが共有する主題の識別子
  Q1   **3個**（request_o2 / llm_r15 / llm_r23）   ← 腕1・腕2 が**両方とも見逃した件**
  Q2   2個（co2_action_margin_kg / exp-017・commit ccda5e5 側）
  Q3   **0個**  ← 届かない。ただし Q3 は腕2 が捕捉した件で、初稿が EXP-031 を名指ししている
```

**⇒ 語彙でも数値でもなく「同じモノの名前を呼んでいるか」で照合する腕は、
   いちばん要る場面（Q1）に届く可能性がある。それが本実験の問いである。**

## 問い

**腕4（主題の識別子の共有）は、Q1 を top-5 に入れるか。そして腕0（定数）に支配されないか。**

## 腕4 の定義（測定前に凍結）

```
入力   クエリ文字列
抽出   CODE = [a-z][a-z0-9]*([._][a-z0-9]+)+  |  [a-z_]+\.(py|sh)     （小文字化して適用）
       長さ 5 文字超・STOP={docs.ja, e.g, i.e, co.jp, flow.engineering} を除く
       ⚠ **引用の識別子（EXP-NNN / *.md / *.yaml / *.json）は使わない。**
         先行文書を名指ししていれば、それは再発見ではなく引用だから
スコア  チャンクとの共有識別子の**個数**（完全一致。EXP-035 訂正6 の部分文字列バグを踏まない）
順位   スコア降順・同点は元の順。上位5件
閾値   m（スコア >= m で発火）を m=1,2,3 で掃引する。**m は事後に選ばない**
```

## 入力（EXP-035 の凍結入力をそのまま使う。作り直さない）

```
正例 3件（Q1/Q2/Q3・会話ログからの verbatim 初稿）
負例 16件（A1〜A3 ＋ negatives_B 13件。**A3 は誤ラベル**と EXP-035 訂正5 が確定済み）
⚠ 負例は汚染されている（EXP-035）。**偽陽性の率は読めない。腕どうしの順序の比較だけが生きる**
```

## 予測（測定前にコミットする）

```
予測1  腕4 は m=1 で Q1 を top-5 に入れる
       ⚠ **正直に言う: 共有識別子が3個あることは既に測ってしまった**（上の表）。
         未測定なのは**順位**である。request_o2 は記録中に頻出するので、
         共有3個でも 1453 チャンク中の上位5件に入る保証は無い
予測2  腕4 は Q3 を取り逃す（共有0 と実測済み）。**これは既知であり、証拠にならない**
予測3  腕4 の偽陽性は腕2 の最良点（2/16）より**多い**。request_o2 / plant_sim は頻出語だから
予測4  腕4 単独では腕0 を支配できない。**腕2 との和集合が腕1 との和集合より良い**
```

## 判定規則（両側に同じ規則を当てる — EXP-035 訂正3 で学んだこと）

```
主指標  正例: top-1 のスコアが閾値以上 かつ 正解が top-5 → 捕捉
       負例: top-1 のスコアが閾値以上 → 発火（偽陽性）
       **捕捉率と偽陽性率を必ず対で報告する。片方だけは無効**（EXP-035 訂正4 で破った規則）
基準線  腕0 定数: 捕捉 3/3・偽陽性 16/16。腕1 / 腕2 は EXP-035 監査後の表を再掲する
n      正例 n=3。**そして正例は互いに独立でない**（EXP-035 訂正5）。**有意性は主張しない**
```

## 撤回条件 — ★ 各条件が「発火しうる入力が実在するか」を先に確かめた

**EXP-035 は、発火しえない条件を1つの事前登録の中で2回書いた。同じ型を踏まないための節。**

| # | 条件 | 発火しうるか（測定前の確認） |
|---|---|---|
| 1 | 腕4 が腕0 に支配される（捕捉で下回り、偽陽性で上回らない） | **YES。** 腕1 が実際にこの形で棄却された（EXP-035 表4） |
| 2 | 腕4 が Q1 を top-5 に入れない | **YES。** 順位は未測定。共有3個でも頻出語なら埋もれる |
| 3 | 偽陽性が 16/16 になる | **YES。** 腕2 は tau=0.05/0.10 で実際に 16/16 だった |
| 4 | 腕4 の識別子抽出がクエリから 0 個しか取れない | **NO — 発火しえない。** Q1=25個 / Q2=4個 / Q3=10個 と実測済み。**この条件は書かない** |

**全腕が同じ採点契約か（測定前の確認）:** 腕0/1/2/4 すべてに
「正例＝正解が top-5 かつ発火」「負例＝発火」の同じ規則を当てる。
**腕0 にも『1件を指させる』制約は課さない**（EXP-035 と同じ扱いのまま。変えると比較できなくなる）。

## この文書の status

**測定はまだ1回も走らせていない**（腕4 のランキングと偽陽性は未測定）。
次のコミットで結果を追記する。順序は S3 事前登録 → S4 測定 → S5 撤回条件の照合 →
S6 独立監査 → S7 再現 → S8 人間の判断。
