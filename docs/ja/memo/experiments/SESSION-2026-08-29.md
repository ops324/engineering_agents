# セッション記録 2026-08-29 — 検証したこと・結果・組み込んだもの

```yaml
期間:      2026-08-29（1セッション）
コミット:  bca25be .. e7e4bd1（**15本**）。すべて push 済み
テスト:    636 → **642 passed** / 5 skipped
公表値:    82.2297 / 68.6661 — **全工程で不変**
GPU:       **0秒**
独立監査:  **7回**（3レンズ並列 × 2 + 単レンズ × 5）
```

## 一行でいうと

**7回結論を出し、7回とも監査が誤りを見つけた。うち4回は公開前に止まった。**
**⚠ この「7回」は分子である。見逃しは未測定（[EXP-036](EXP-036-what-the-audit-missed.md)）。**
**公表値は一度も動いていない。GPU は1秒も使っていない。**

---

## 1. 検証したこと と 結果

| # | 検証したこと | 結果 |
|---|---|---|
| 1 | 案3（水のために運用点を締める）は成立するか | **保留。** 定数が 80/80 を取り、窓を延ばすと全滅（EXP-028→030） |
| 2 | 生存者数は打ち切りカウントか | **その通り。** 案3 の運用点は steps=100 で全滅（EXP-030） |
| 3 | それは本題（EXP-013/014/015）に効くか | **見出し①は無傷、見出し②は窓 50 の産物**（EXP-031） |
| 4 | 観測窓を 72（＝1日）にすべきか | **否。分解能は1ミリも増えない**（14〜150 step で常に「4 対 3」）。戻した（EXP-032） |
| 5 | `request_o2` は交絡か | **罠だった。** 純粋なシンク。ルール腕は構造上出せない（EXP-033） |
| 6 | 罠を除くと LLM は no-op と同じか | **否。有意に勝つ。** LLM は no-op とルール腕の**中間**（EXP-033） |
| 7 | LLM の「早さ」は利点か | **否。** 効くのは「step13 までに撃ったか」の二値。早撃ち群が最終生存で最下位（EXP-034） |

### 主要な数字（すべて再現済み）

```
案3            (0.08, 0.0, 0.6) の定数が 20セル全部で 4/4。著者の「上限」は 71/80
窓             steps 20/36/50/72/144 すべて「ルール腕 4 対 no-op 3」。200 で初めて 2 に落ちる
本題の見出し②   W=15 では3世代とも LLM が有意に**良い** / W=50 では区別できない〜悪い
request_o2     使用13本 O2死 2.08/本 / 未使用59本 0.24/本（**8.8倍**）。v5 未使用20本は **0**
LLM は中間      person-steps +2.30 p=0.0068 / peak CO2 -0.47 p=0.00014 / **A軸 +0.245 p=0.000065**
早さ           step≤12 群の最終生存 **2.70** < step13 群 3.00、step≥14 群 2.80
密度           有効 step 数 中央値 **8.5** 対 ルール腕 37（生本数 11.5 は空撃ち込みで水増し）
```

---

## 2. 組み込んだもの（コード・計器）

| コミット | 内容 | 検証 |
|---|---|---|
| `12555e5` | **ガードの穴10個目を塞いだ** — `arm_modified` を新設。`agents` 部分木がどちらのフラグの差分対象にも入っておらず、腕を調整して +3.13点が無記録で取れた | 攻撃2本＋誤検知2件を回帰テストに固定。**公表値不変**。636→640 |
| `7a65a27` | **本番経路のバグを直した** — provenance の突合せが `"o2" in text` で判定し、`request_o2` 36件中5件が O2 に触れないメッセージに紐づいていた（'ars' も "clears" に3件） | 語境界判定に変更。**誤一致 0件**。回帰テスト2本。640→642 |
| `eeba123` | STATE.md から腐る数字（コミット数・未 push 数）を削除し、確認コマンドに置換 | — |

**未修正で残っているもの**（記録済み）:
```
request_o2 の「rejected でも酸素が抜ける」  実バグ。plant_sim と mock で契約が食い違う
                                        → **チーム報告書の判断4 に提出済み**
graph_rewire が3つのガードを素通り        ros2 専用で plant_sim では無害（既知）
thresholds.o2_storage_low_kg の二重の役割  物差しと腕のトリガを兼ね、片側しかラベルされない
```

---

## 3. 訂正したもの（公開済みだったもの）

```
EXP-028  「案3 は成立しない／残る道は R4 のみ」        → **撤回**（EXP-029）
EXP-029  「案3 は成立する」                          → **撤回**（EXP-030）
EXP-030  「この問いは EXP-013/014/015 にも立つ」       → 過大ではなかった（EXP-031 で確認）
EXP-024  「designer 層には代償がある」                → **帰属の誤り**（経路は乗員の死）
EXP-013/014/015  「LLM は no-op と区別できない」      → **窓と粗い指標の産物**。但し書きを追記
チーム報告書 判断1                                   → 二度誤った内容で公開し、二度訂正
```

---

## 4. ★ 繰り返した失敗（次セッションが最も注意すべきこと）

### ① 既存記録の再発見 — **3回**

```
1回目  v5 README の「交絡除去で符号反転 +0.246 / p=0.000065」を EXP-033 初稿が打ち消した
2回目  co2_action_margin の掃引が ccda5e5 のコミットメッセージに全部既出だった
3回目  EXP-034 の「早さ」の分解が EXP-031 / commit 620dab1 / window-review README /
       next-session-prompt の4箇所に既出。有効 step 数 8.5 も pr-body-draft:112 に既出
```

**そして規律「自分の枝の既存記録を先に grep する」は、next-session-prompt:121 に本人が本日書いた。**
**書いた当日に3回破った。規律を書くことと守ることは別の行為である。**

### ② 設計判断を「見落とし」と呼ぶ — 2回

```
OGS のラッチ    「バグに見える」→ 3cf84ea が名指しで検討して残した設計だった
3.03点          「取り逃している」→ 3cf84ea が理由つきで見送った設計判断だった
```
**どちらもコードを2行引用して、周辺とコミット履歴を読まずに書こうとした。**

### ③ 誤りの向きが毎回入れ替わる

```
EXP-028  「4回落ちたから5回目も落ちる」→ 否定に寄りすぎ
EXP-029  その反動で肯定に寄りすぎ
EXP-031  自分の直前の主張を取り下げすぎ
EXP-034  「早さに効果がある」→ 実は効果ゼロ
```

### ④ 部分文字列で判定した

`"o2" in "co2_warning"` が真で、CO₂ 死を O₂ 死として数えていた。
**直すと結論の向きが逆になった。** 同型のバグが本番コードにもあり、修正した（`7a65a27`）。

### ⑤ 監査官が制約を2回破った

```
1回目  リポジトリに 365行の文書を書き込んだ（退避済み）
2回目  **著者の commit を amend した**（禁止と明示していた）
```
**そして著者は `git add -A` の前に `git status` を読まず、監査官が書いた 697行を未読でコミットした。**
→ STATE.md の手順ルール7・8 に格上げ済み。

---

## 5. 効いた手順（守れたもの）

```
★ 測定 → 監査 → 訂正 → 公開 の順序   EXP-030 以降は守れた。**誤った結論が公開状態にあった時間ゼロ**
★ 事前登録（予測・判定規則・撤回条件）  撤回条件が実際に発火して判断を止めた
★ 監査官への「偏りの向き」の明示       毎回向きが違い、3体とも別々の欠陥を見つけた
★ 監査を鵜呑みにしない                 監査の指摘のうち、再現しなかったものが2件あった
★ commit 前に git status を読む        1回破って事故になった。以降は守った
```

---

## 6. 次にやること

**設計方針を [flow-engineering-design.md](../flow-engineering-design.md) に起草した（未監査・未実装）。**

```
1  本日の EXP-028〜034 と既存の EXP-000〜027 をグラフに投入する
   → **すぐ効果が測れる**: 「本日の3回の再発見は S2 ゲートで止まったか」
2  止まったなら S2（既存記録の照会）を実際のゲートとして実装する
3  監査の3レンズをテンプレート化（偏りの向きの明示を必須項目に）
4  ECLSS でフローを回し、捕捉率を測る
```

**ECLSS 側で未決のもの**:
```
案3 の成否           保留（★保留理由は EXP-037 で解消。「窓を倍にして生き残るか」で足りる）
案2（電力の共有）     一度も測定していない
R4（リーク＋対処）    「残った唯一の道」は撤回済み
チーム判断4件         request_o2 / 窓 50 の根拠 / ルールの刻み ×0.9 / 採点の重み
```
