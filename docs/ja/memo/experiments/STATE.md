# 現在地

セッションを跨ぐための状態。**次に読むべきものと、次にやるべきこと。**

**最終更新: 2026-08-29（EXP-029・独立監査が EXP-028 を撤回させたところまで）。**
ファイル名には日付を入れない — 作業のたびに書き換える1枚であって、日付つきの名前だと
中身が更新されても名前が古いままになり、どれが現在地か分からなくなる（実際に一度なった）。
過去の時点の姿が要るときは git 履歴を見ること。

## ブランチ

```
experiment/eclss-evaluation-layer
  **コミット数と未 push は書かない — コミットのたびに腐り、実際に一度腐った。**
  ここに数字を書く代わりに確かめること（どちらも1秒）:
    git rev-list --count d5616389..HEAD
    git log --oneline origin/experiment/eclss-evaluation-layer..HEAD
  土台: d5616389（#58）。**upstream/trunk は c7f3d89（#59）に進んでいて未取り込み**
        → #59 はルール腕を「1 step に ceil(不足/効果) 回」に変えたが、この枝の定格の
          不変条件が同一 step の合算を1回分で止めるので**効果ゼロ**（EXP-016 の確認と同じ根拠）。
          仮想マージの衝突は 2ファイルのみ（ssos_eclss_loop_team.py / agents.yaml）
  push 先: origin (ops324/engineering_agents)。upstream は push 無効
  PR: 作っていない（フロー上、実験段階では作らない）
```

**この枝は本人が main から切って独自に改造・検証しているもの。**チームの合意待ちではない。
ただし**この枝で選択したもの**（乗員4、サバイバル帯の分離、A〜D の配点式）は、
チームの基準ではないので、報告時に区別すること。

```
テスト: 640 passed / 5 skipped / 0 failed（2026-08-29 実測。arm_modified の回帰4件を追加）
        ※ かつてここには「587 passed / 14 failed」と書いてあったが、古い記述だった。
          2026-08-27 に実測したところ、変更前のベースラインは 601 passed / 0 failed。
          14件の失敗は再現しない。**それでも差分で見る習慣は変えない**
mkdocs build --strict: 通過
```

## この枝で何をしたか（全部 EXP に記録済み）

1. **測定器を trunk に載せ替えた** — physics_gate / reference_limits / trajectory_metrics / proposal_evaluation
2. **運用点を測り直した** — trunk（乗員50）は全 run 全滅で判別不能（EXP-007）
3. **既存 run は採点できないと判明** — 861 run が telemetry を持たない（EXP-008）
4. **乗員を4人にした** — 装置の定格（ARS 4.33人分）と容積（97 m³/人）に合わせた（EXP-009）
5. **提案が死者を消せる穴を塞いだ** — 評価層（案1）＋発生源（案2、`plant_sim.survival.bands`）（EXP-010）
6. **ノイズ床を測った** — 必要 n は 18〜26/arm（EXP-011 の「7」は過小評価、EXP-012 で訂正）
7. **温度は効かないと判明** — EXP-011 の観察は追試されず（EXP-012）
8. **スコアカードを実装した** — `ea scorecard`。A〜D の配点式はこの枝の選択
9. **本題に答えた** — ルール腕が有意に優位、LLM 腕は no-op と区別できない（EXP-013）

## いちばん大事な結果（EXP-015、世代 v5・2026-08-28）

```
共通3軸（90点満点）
  ルール腕 82.230   LLM 66.257 (n=24, SD 9.25)  差 -15.97  p<0.000001  → 有意
  no-op腕  68.666   LLM 66.257                  差  -2.41  p=0.215     → 区別できない
```

**3世代（v3・v4・v5）で同じ結論。** 酸素の物理を直しても、情報格差を埋めても覆らなかった。

**v5 で新しく分かったこと:**

- **`request_o2` は誤設定の産物ではなかった。** キャビン 105 kg でも腕は自分で酸素を抜いて
  乗員を殺す（2本）。**危険なのはレバー**であって、タンクが 8 kg だったからではない。EXP-014 に注記済み
- **交絡を除くと符号が反転する。** request_o2 不使用の20本は no-op を **+0.246点 有意に上回る**
  （p=0.000065）。ただし 90点満点中 0.25点。**「有意」と「意味がある」は別**
- **O₂ 軸では LLM がルール腕に勝てる。** OGS 10回以上の10本は帯割れ中央 5/50（ルール腕14）。
  **能力の問題ではなく一貫性の問題** — run ごとに配分がばらばら（ARS0/OGS45 も ARS37/OGS1 もある）
- **得点のほぼ全部が「乗員1人」で決まる。** ARS を20回以上出しても0回に対し 0.8点しか改善しない。
  差13.6点の内訳は残存12.5点＋その他1.1点。**R3（重み）の問題が結果に出ている**
- **上側の峰が消えた。** v4 では llm_r12 が 81.995 でルール腕に並んだが、v5 では1本も無い

## 次にやること

**道順は [ROADMAP.md](ROADMAP.md) に分離した。** ここは現在地、あちらは順序と理由。

```
R1  水の基準値 [V2 6109] を出典から取る   ✅ 完了（2026-08-28）
R2  酸素をキャビン大気に（リークは入れない）✅ 完了（2026-08-28）
R3  採点式に根拠を与える                   調査済み・**チーム判断待ち**（R6 の障害ではなくなった）
    → R3-scorecard-weights.md。重みは出典から引けないと判明（価値判断であって測定値でない）
R4  リーク＋対処手段                       ⏸ 将来フェーズ
R5  測定 v5                               ✅ 完了（EXP-015）
R6  ハイブリッド腕（Flow Engineering 型）  ⏸ 判断点が全て退化（EXP-017）。先に「置く場所を作る」
```

**⚠ EXP-016 の結論は撤回した（独立監査3体・全件再現して確認）。**

「分解能は C・D にある」は誤りだった:
- **D は採点の取りこぼし**（`rated_step_capacity` が飽和理由に無い）。修正して3世代とも 5.000・SD 0 に戻した
- **C は判断の質を測っている証拠が無い**（発行指令数と r=−0.629、no-op と軌道が同一の3本に 0.00〜2.72 の差、
  5点中1点はスキーマ適合度、過小要求は常に満点）

**行き先は分かった。余地は B 軸の CO₂ 側にある**（co2_worst 0/4・co2_terminal 0.24/4、
ピーク 2.05 kg が帯 2.0 を超えている）。**規格に紐づき、「いつ撃つ・いつ止める」で動く。**
**ただし到達可能なのは 3.03点**（0.238 → 3.263）。7.76 は満点までの差であって余地ではない — 下の EXP-017。
R6 設計は第2版に差し替えたが、**その第2版も EXP-017 で退化した** →
[r6_hybrid_arm_design.md](../ssos_eclss_loop/r6_hybrid_arm_design.md)

**そして判断点 f も退化していた（EXP-017・実装前の掃引で判明・GPU 0秒）:**

```
撃つのは無料           → 常に撃つのが最適。内点の最適が無い（マージン1.0 で ARS 50/50）
到達可能な余地は 3.03点  → 「7.76点」は満点までの差であって余地ではなかった（自己訂正）
行動数の上限が効かない   → 1でも3でも指令81件。ARS と OGS を competing させられない
故障に対処手段が無い     → 診断しても結末が変わらない
O₂・水は窓内で帯に届かない
```

**この運用点で効くのは「ARS を持続的に回す」ことだけで、それは定数政策。**
＝ **判断を置く場所が無い。腕ではなく運用点の性質である。**
「LLM は no-op と区別できない」に別の説明を与える — **決めることがほとんど無い。**

→ **次は「AI をどこに置くか」ではなく「置く場所を作る」**（EXP-017 の4案）

**そして案1 も実装前に落ちた（[EXP-018](EXP-018-scarcity-without-value.md)・2026-08-29・GPU 0秒）:**

```
指令の54%は0点     → OGS 14 + WRS 30 を無効化しても共通3軸は 82.2297 で小数4桁まで一致
帯が遠い           → B の O₂ は発火線 104.25 でなく生存帯 91.31 で採点。余裕 11.3 kg
予算が縛らない     → ARS は常時運転の極限（margin 1.0）でもちょうど 1/step
```

**希少性は「奪い合うもの」を作るが、奪い合う価値が片側にしか無い。**
f は代償が無くて退化し、案1 は**代替の使い道に価値が無くて**退化した。症状は同じ「内点が無い」。

**そして案4 も実装前に落ちた（[EXP-019](EXP-019-failure-has-no-ambiguity.md)・2026-08-29・GPU 0秒）:**

```
故障は賭け金を4倍にする → 82.2297 → 69.7382（乗員 4→3）。余地 3.03点に対し 12.49点
期限は3 step の崖     → step13 までなら全員無事、14 で 12.37点が一度に落ちる
だが曖昧さが無い      → ARS 健全 −0.0047 / 故障 +0.0578（39/39・ゆらぎゼロ）
                       観測に ars_failure_enabled が平文。隠しても引き算1回で分かる
→ 診断が判断にならず、曖昧さが無いので「すぐ直す」が支配的。また定数政策
```

**同じ壁に3回ぶつかった。** f は撃つのが無料、案1 は代替に価値が無い、案4 は曖昧さが無い —
言葉は違うが**壁は同じ**。困るものが CO₂ しか無く、CO₂ の正解は常に「ARS 全力」だから。
**判断が生まれるには正解が状況で変わる必要があり、この運用点では変わらない。**

**そして案3 に入る前に、前提が1つ削れた（[EXP-020](EXP-020-o2-resolution-is-discarded.md)・再採点のみ・無料）:**

```
O₂ 軌跡に分解能がある  → OGS を回すかで規格の帯 104.25 を割る時間が 14 対 36 step（2.6倍）
   採点が捨てている    → B/A は survival 帯 91.31 を見ており、そこは一度も割らない
   帯を揃えても不足    → 式が「深さ」しか測らず、分解能は「時間」にある。揃えて +0.07点
水 軌跡にも分解能が無い → 50 step で一度も 50 L を割らない
```

**同じ B 軸の中で、CO₂ の余裕は運用の帯まで、O₂ の余裕は死ぬ帯まで測られている**
（CO₂ は `yardstick`＝thresholds、O₂/水 は `survival bands` から取る。水と CO₂ は両者同値）。

**⚠ ここから先は物差しのハザードそのもの。** 「軸が動かない→採点を変える」は3回間違えた形。
EXP-020 は事実の報告に留め、**採点式を1文字も変えていない。**
**判断の前に原則4 の独立監査を通すこと**（発動条件に完全に当てはまる）。R3 の領域でもある。

**含意: 案3（運用点を締める）が要るのは水。O₂ は採点の問題を含む。**
案2（電力の共有）は同じ理由で落ちる公算が大きい。

## ⚠⚠ EXP-021 — 監査で EXP-020 を撤回し、**ガードの破れを3件見つけた**（2026-08-29）

独立監査3体（互いの結論を知らせず並列）。**指摘は全件こちらで再現した。**

**撤回:「分解能は深さではなく時間にある」は逆だった。**
帯を 104.25 に揃えれば深さの式だけで 3値 → 18値（幅 0.175 → 1.059点）、生存との相関 +0.924。
提案しようとした「時間」の項は **乗員が死ぬと点が上がり**（+0.202）、
OGS 指令数と 60本中50本で**整数一致**。EXP-016 の C 軸と同じ失敗の型。
算術も 1項落ちていた（0.0705 → 正しくは 0.1186）。

**★ 提案とは無関係に、いまの計器に生きた穴が3つある（全件再現済み）:**

```
破れ1  plant_sim.survival の滞留表が無防備（*_loss=0 / divisor=99999）
       乗員 2/4 → 4/4、+24.33点、**しかも CO₂ ピークは 8.81 → 12.06 と悪化**。ガード []
       → EXP-010 の穴が bands の隣で開いている。**3度目の「1フィールド隣」**
破れ2  既定の --yardstick standard がガードを一度も呼ばない（score.py:401 は run 分岐の中）
       **拒否メッセージが standard を避難先として案内する。そこが無防備**
破れ3  何もしない腕が 90.000/90 満点を取れる（initial_* と crew.activity_factor・ガード []）
       ルール腕の公表値 82.230 を 7.77点上回る
```

**✅ 破れ1〜3 は修正済み（`653acdf`）。** 攻撃を再実行して塞がったことを確認:

```
ガードを plant_sim.survival 部分木の差分に（列挙をやめた。次に増える鍵も自動で covered）
両方の物差しでガードを呼ぶ（survival は standard でも拒否。thresholds は standard で通す）
operating_point_modified を新設 — 運用点の変更を記録・表示し scorecard.json にも載せる
公表値は不変（v5 参照腕 82.2297 / 68.6661）。テスト 619 → 623 passed
```

**未修正:** 破れ4（`--habitat-volume-m3` は採点時指定なので構造上捕捉不能）、
破れ5（ガードは `summary.json` の自己申告で証拠ではない）、
副産物（`summary["plant_sim"]` 未書き込みで C 軸 latency の分母が常に既定値）。

## ⚠⚠ EXP-022 — 「帯を取り直す」は前提が誤り。そして窓が4つ空いていた（2026-08-29）

[EXP-022](EXP-022-the-guard-had-four-more-windows.md)。独立監査3体・**指摘は全件こちらで再現。**

**前提の訂正: 採点が読む O₂ の帯は `thresholds.o2_storage_low_kg`（104.25・提案が動かせる）ではなく、
`plant_sim.survival.bands.o2_storage_low_kg`（91.31）である。** 後者は ALLOWED_SET の外で、
`653acdf` のガードが見張っており、監査が全経路で動かそうとして**全滅した。**
`reference_limits` に移すと**容積依存になり、いまより弱くなる。**

**そして却下しようとした設計文書のほうが誤っていた（自分で撤回）:**

```
「生存24本の幅 0.075点」→ **0.346点**。llm_r20 が集計から落ちていた
                          しかも「生存しながら酸素だけ壊滅」という、拾うべき当のケース
同じ検定を現状に未適用   → 当てると現行の帯は生存24本を **1本も分離しない**（幅 0.0000）
                          提案の帯は OGS を回す腕と回さない腕を **p=0.00010** で分ける
反例が都合の良い1本       → llm_r9 は OGS 40超の3本で唯一 request_o2 で自傷した run
```

**★ 提案と無関係に、生きた穴が4つあった。うち3つは塞いだ:**

```
破れ6 ✅ --steps が両ガードに無い → 何もしない腕が 2 step で 86.1/90（公表ルール腕 84.35 超）
        警告ゼロ・フラグ1本。EXP-021 の 90.000点より安い。simulation.steps を無視集合から外した
破れ7 ✅ 容積に入力検証が無い → -5 m³ で B軸満点、0 は黙って 388 に戻る。_habitat_for() に集約
破れ8 ✅ 既定の standard がガードを表示しない → 警報線を 99999 にして +3.84点、画面に何も出ない
破れ9 ✅ 容積を **拒否側**に置いた。scenario.yaml に plant_sim.habitat.volume_m3: 388.0 を新設し、
        _scoring_bar_modified が差分を取る。偽れば両方の物差しで採点不能。
        採点は run の申告を既定にし、--habitat-volume-m3 で上書きするとカードに
        「run declares 388 m3 -- overridden」と出る
破れ5 ✅ 帯を scenario_config.yaml と照合。不一致なら拒否（監査官の偽造コピーは実際に拒否された）
        ~/ea-runs の 168本は全て一致、532本は照合材料なしで bands_verified: null
公表世代 ✅「unverified」の注記を両方の物差しに出す（これまで run 分岐の中だけだった）
副産物 ✅ C 軸 latency の分母を config から取る（summary["plant_sim"] 未書き込みで常に既定値 2 だった）
陳腐化 ✅ R2 以降 偽だった記述を5箇所＋ scenario.yaml の 88.31 → 87.71
```

**公表値は不変**（82.2297 / 68.6661）。**テスト 623 → 631。** commit `944bbe0` / `5224396` / `0d17616` / `3be366f`。

## ⚠⚠ EXP-023 — 監査3巡目。**自分で先に書いた撤回条件が、実装前に発火した**（2026-08-29）

[EXP-023](EXP-023-the-rule-i-wrote-fired.md)。3体とも独立に「採用してはいけない」。全件再現済み。

```
撤回条件1 発火  提案の帯は**乗員が死ぬと点が上がる**。死の報酬 +0.0234点 は
                EXP-019 で提案が稼ぐ分解能の全幅 0.0237 とほぼ同じ。交差対 731 中 535 で逆転
                機構は物理（O₂ 需要 ∝ 生存乗員）。**CO₂ には因果があるので起きない。O₂ に固有**
撤回条件4 発火  --set simulation.initial_o2_storage_kg=106.2 のノブ1本で
                no-op が 9.9090 → 10.0000 とルール腕 9.9984 を追い越す。ガードは表示のみ
                **★ 現行の帯では同じ攻撃の差が 0.0000。飽和は免疫でもあった**
基準2 破綻      p<0.01 は原理的に到達不能（OGS=0 群6本のうち5本が同一軌跡＝擬似反復。
                畳むと最小 p = 1/66 = 0.0152）。カット次第で p は 0.485〜0.0001 に動く
基準4 無内容    B軸O₂ は軌跡の純関数なので、同一軌跡の分離は数学的に不可能＝反証不能
基準1・2 が値を選べない  出典の無い 110 kg のほうがよく通る（19値/0.3820/+0.0762）
私の誇張        「91 run すべてで1値」→ 3値。正しくは「生存89本で1値」
                **EXP-022 が1日前に訂正したのと同じ2本・同じ型を、判断を運ぶ地の文で再掲**
```

**結論: 帯は 91.31 のまま。採点式は1文字も変えていない。公表値も不変。**
`scenario.yaml:129` と `scorecard._score_a` が既に「R4 まで O₂ は動かない。それは意図」と書いていた。

**残した代替（ゼロリスク・3体中2体が独立に提案）:**
**採点は変えず、104.2492 に対する値をカードに参考として併記する。**

**先に塞ぐべきもの（本提案より大きい）:** `simulation.initial_o2_storage_kg` と
`plant_sim.crew.{size, activity_factor, o2_kg_day_person}` は記録されるだけで拒否されない。
いまは O₂ 軸が飽和しているので実害が出ないが、**飽和に依存した安全性である。**

**教訓（EXP-022 の続き）: 「両側に当てる」だけでは足りない。「全集合に当てる」まで要る。**
基準1・2 は両方の帯に当てたが、基準3・4 は v5 にしか当てなかった。EXP-019 に当てれば落ちた。

## ★ EXP-024 — 誰も回していなかったループを回した（2026-08-29・GPU 0秒）

[EXP-024](EXP-024-the-loop-nobody-ran.md)。**予測を測定前にコミットし、3つのうち2つ外れた。**

実態は「d は未実装」ではなかった:

```
提案を作る側   design_mode=labeled_rule_base で 120 run 実行済み・全て design_proposals.json あり
提案を検証する側 proposal_evaluation.json は ~/ea-runs 全体で **0件**。無料なのに一度も回していない
LLM designer   design_mode=llm は **0件**
```

**71本を評価した（49本は乗員50世代なので不変条件が正しく拒否）。結果:**

```
★ 効くレバーは1本だけ  thresholds.* を含む提案 60本 → improved 38/mixed 17/no_effect 5
                       含まない 11本 → **no_effect 11/11**
                       action_profile 125件・service_config 65件は**全て無効**
                       （EXP-018 の「指令の54%は0点」が designer 層で再現）
★ 乗員が動いた         10本で 3/4 → 4/4（+12.5点）。**R6 方向で 50点軸が動いたのは初めて**
                       全て thresholds.co2_storage_high_kg を下げる提案・全て故障注入下
                       機構は素直（警報線↓ → ARS が早く発火 → 故障の3 step の崖を越える）
★ 計器どうしが食い違う  乗員を救った10本は**全て verdict が mixed**。
                       水が 0.02 L 減るだけで mixed になる。evaluate は9指標を等重み、
                       スコアカードは乗員が 90点中50点。**観察に留め、触らない**（R3 と同性質）
★ 代償が現れた         CO₂ の警報線↓ → ARS 増 → 水が減る（55本中13本で悪化）。
                       **EXP-017「代償が無い」は actor 層の話で、designer 層には代償がある**
                       ⚠ **撤回（EXP-028）。経路は ARS ではなく乗員の死。**
                       乗員固定なら ARS を35%増やしても水位は小数4桁まで同一。
                       水が減ったのは「救われた乗員が水を飲んだ」から。代償ではない
```

## ⚠⚠ EXP-025 は第1版・第2版とも撤回（2026-08-29）→ [EXP-026](EXP-026-the-metric-a-constant-string-wins.md)

```
第1版  ルール designer 対 LLM designer の**競争**設計。ユーザーの指摘で撤回
       （ROADMAP が「あるべき姿ではない」と書いた形。答えの出た問いを1階層上でやり直していた）
       撤回のきっかけ: **designer のプロンプトに ALLOWED_SET_PARAMETER_TARGETS が無い**。
       4時間かけて「LLM がうちのパラメータ名を当てられるか」を測るところだった
第2版  判断点を1つずつ渡す梯子。**独立監査3体が全員「測ってはいけない」。全件再現して撤回**
```

**主指標が判断を測っていなかった:**

```
run を一切読まない定数1行 {"target":"thresholds.co2_storage_high_kg","value":1.8} で **救出 10/10**
value=true / -5.0 / 0.0 でも救出する。＝「ある欄に 1.8 以下を書いたか」の1ビットだった
graph_rewire は中身を見ずに通り、ros2 専用で plant_sim に届かない
  → probe の真の無効率は 79% ではなく **14/14 = 100%**
```

**さらに重い3つ:**

```
★ LLM が JSON を吐き損ねると**ルールの正解が黙って代筆**され、それは 10/10 救出する
  → LLM の完全な失敗が「棄却0%・thresholds提案・乗員救出」として記録される
  → 腕2 はプロンプトが長い＝失敗しやすい＝**著者の望む結論が出やすい**交絡
★ designer のプロンプトに **crew / 乗員 / occupant が1語も無い**。主指標は乗員救出なのに
★ 渡す予定の10件のうち **6件は merge_labeled_policy_from_thresholds に上書きされる死物**
```

**著者の誤り:**「うち約1.5分が simulation」→ 実測 **0.096秒**（約1000倍）。
「腕2 でスキーマを足す」→ 既にプロンプトに入っている。
「EXP-024 が action_profile/service_config を無効と測定」→ 単独検証は 11件/9件のみで**外挿だった**。

**GPU は probe 1本（4分27秒）のみ使用。4.5時間は払っていない。直すものは全て GPU 0秒。**

**新しい規律: 主指標を決めたら、測る前に「判断ゼロの入力」を通す。**
定数1行で満点が出るなら、その指標は判断を測っていない。**30秒・無料で分かった。**

**計装と指標は修正済み（`152013c` / `1327bac`・GPU 0秒）:**
棄却理由と payload を保存／全件棄却でも記録／`set_parameter.value` を検証／
**designer に乗員を渡す**（主指標は乗員救出なのに、プロンプトに crew が1語も無かった）／
主指標を「有効提案率」に（`analysis-tools/effective_rate.py`）。

**ルール designer の基準線（実測・71本）: 有効提案率 309件中 55件＝17.8%。**
`action_profile` 0/125・`service_config` 0/65・`agents.*.policy.*` 0/59 は**実測で無効**
（EXP-024 の外挿を置き換えた）。効くのは `thresholds.co2_storage_high_kg` の 55/60 だけ。
**probe（LLM designer）は 0/14 = 0.0%。**

## ⚠⚠ EXP-027 — 設計の層にも内点が無い。同じ壁の4回目（2026-08-29・GPU 0秒）

[EXP-027](EXP-027-the-designer-layer-degenerates-too.md)。**設計文書を書く前に前提を確かめて、成立しないと分かった。**

```
ルールは 27本ぶん取りこぼしている  exp019 で乗員を失った 43本のうち、ルールが救えたのは 10本
                                  残り33本に深い刻みを当てると 1.0 で **27/33 救出**
                                  取りこぼしの原因は「刻みが ×0.9 固定」＝**量**の問題
だが、そこに判断は置けない        **1.0 と 0.5 が全指標で完全に同一**
                                  ARS 指令数も **33/33 で同一** ＝ 飽和している
                                  警報線がある値より下では ARS は毎 step 発火し、以降は不変
```

**⇒ 定数1行 `{"target":"thresholds.co2_storage_high_kg","value":1.0}` が、
ルールの designer を 27本ぶん上回る。有効提案率は 100%（ルールは 17.8%）。**
**「量を渡す」実験（EXP-025 の腕3）は成立しない。**

```
EXP-017 f       撃つのが無料 → 内点が無い
EXP-018 案1     あきらめる側に価値が無い → 内点が無い
EXP-019 案4     曖昧さが無い → 内点が無い
EXP-027 設計層  **切りすぎる罰が無い → 定数が最適 → 内点が無い**
```

**退化は腕の性質ではなく運用点の性質で、層を上がっても消えない。**

**AI と無関係な発見:** ルールの designer は刻みが ×0.9 固定なせいで 27本の救出を取りこぼしている。
ただし「飽和点まで下げる」が正しい運用かは R3 の領域で、この枝が単独で決めない。

## ⚠⚠ EXP-028 は撤回した → [EXP-029](EXP-029-the-audit-broke-exp-028.md)（2026-08-29）

**★ 案3 は成立する。締める相手を間違えていただけだった。** 独立監査3体が互いを知らずに
同じ欠陥を指摘し、**全件を著者のコマンドで再現した。**

```
初期O2 92.0 kg ＋ 初期水 52.0 L を**同時に**締め、OGS 要求量を振る
  0.000 → 生存 0（O2 で全滅）  0.075 → 生存 4/4  0.150 以上 → 生存 0（水で全滅）
  scoring_bar_modified = []
機構は物理: 電気分解が可飲水を食って O2 を作る。回さねば O2 で死に、回しすぎれば水で死ぬ
**案3 の原文は「O2 と水が窓内で帯に届くように」。水だけ締めて案3 を否定していた**
```

**EXP-028 の誤り（すべて再現済み）:**

```
格子が結論を作った   窓は 0.5〜0.8 L。それを **1 L 刻み**で測って「0/8 に消えた」と書いた。
                    細格子では消えず、漏れを強めるほど **0.5 → 0.8 L に広がる**
飽和領域だけで測った  「ARS に水のコストが無い」の表は co2_high 0.5〜2.0 の中だけ。
                    広げると ARS は Sabatier 経由で **1.012641 L/50step 作る**（死1人分の2.1倍）
                    **自分で書いた撤回条件3 を、発火すべき場面で発火させ損ねた**
既定値が最適も偽     細格子では co2_high=1.5 が既定値 2.0 に乗員1人分きっちり勝つ行がある
ループが閉じている も偽 400 step まで傾き一定で漸近の兆候なし。正しくは「排水が窓より19倍遅い」
                    O2 の 2230 step も過渡混じりの外挿で、正しくは約 3330
既存パラメータで届く  `activity_factor=10` だけで最小水位 48.771 L・水で2人死亡
                    → **「R4 は代謝に紐づかない新しい項が必須」は撤回**
探索空間が狭すぎた   ALLOWED_SET の閾値4本のうち **1本しか掃引していない**。
                    `thresholds.o2_storage_low_kg`（OGS の発火点）は未掃引だった
事前登録の自己採点   撤回条件6 は、それが却下する内点を見た**後**に書かれていた。
                    追記1 の判定規則をそのまま当てれば掃引C は「成立」だった
```

**★ ガードの穴10個目（強度「弱」）→ ✅ 修正済み `12555e5`:** `agents` 部分木がどちらのフラグの差分対象にも入っていなかった。

```
                        総合点     C軸     最小水位L   両フラグ
既定                   91.5461   4.316    78.163     [] []
co2_action_margin=1.2  94.6737   4.411    78.163     [] []   +3.13点
wrs_feed_trigger=9999  92.2102   4.981    72.781     [] []   +0.66点
→ **水の回収を止めた run のほうが点が高い**（C 軸の request_sizing のせい・EXP-016 の新実例）
→ **`arm_modified` を新設した。** 腕の選択（`agents.mode` / `actor.mode` / `design.mode`）は
   無視し、政策（`agents.actor.policy.*` と旧別名 `agents.policy.*`）を鍵の名前まで出す。
   **拒否ではなく記録**（腕の調整は正当）。テスト 636 → 640。公表値は不変
```

**教訓:** 掃引の刻みは探している構造の幅より細かくする／飽和を「内点が無い」の証拠にしない／
提案の原文が名指ししたレバーを測らずにその提案を否定しない／事後に書いた条件を事前登録と呼ばない／
**負の結果が4回続いた後の5回目は、筋書きが先にできていると疑う。**

**そして順序を破った。** EXP-028 は監査を**通す前に** commit・push し、ROADMAP と記憶まで
書き換えた。原則4 はまさにそれを止めるためのものだった。

---

## （以下は撤回された EXP-028 の当時の記述。何をどう間違えたかの記録として残す）

### ⚠⚠ EXP-028 — 案3 も成立しない。ループが閉じているから（2026-08-29・GPU 0秒）

[EXP-028](EXP-028-the-loop-is-closed.md)。**設計文書を書く前に前提を確かめて、成立しないと分かった**
（EXP-027 と同じ規律の2回目）。決定的な 789 run・数分。事前登録の**予測は2つとも外れている。**

**決定打は1行:**

```
乗員4を固定して ARS 指令を 50 → 37（35%減）にしても、水位は 78.1632 で小数4桁まで同一
＝ **ARS に水のコストは存在しない。** 案3 が前提にしていた代償が無い
機構はコードのとおり。ARS は水を使わない。捕集 CO2 は Sabatier の原料で、Sabatier は水を作る側
```

**CO2 の判断と水を結ぶ経路は「乗員の死」1本だけだった:**

```
乗員 4→3 で 50 step の水の減りが 0.4833 L 小さくなる（水需要 ∝ 生存乗員）
＝「早めに1人を CO2 で死なせると、残りが水で死なずに済む」
**EXP-023 で撤回した「乗員が死ぬと点が上がる」と同じ構造。今回は採点式でなく物理**
```

**この枝で初めて内点が出た。だが幅が「死1人分」しかなかった:**

```
故障あり・初期水量 52 L で 生存 1,1,1,1,3,3,3,3,2,2（両端が中央より悪い＝内点）
だが窓は 51.75〜52.0 の **0.5 L**。乗員1人の死が節約する水 0.48 L とちょうど同じ
窓の中でも 1.5/1.8/2.0/2.5 が同点で、**既定値 2.0 がその中にある**＝何もしないのが最適
形は「勾配の綱引き」ではなく **崖と崖**。間に出る内点は測度ゼロで、平坦だから分解能が無い
```

**締め方を変えても、窓を延ばしても駄目だった:**

```
WRS 定格を27分の1に      乗員は 10×10 セルすべて同一
リサイクル全廃（漏れ上限） 50 step で 80.0 → 73.07 L。**帯の 23.07 L 手前**
在庫と速度を両方締める    内点セルは 1/8 → 0/8 に**減る**
観測窓を8倍（400 step）  水 67.37 L・O2 102.43 kg。**最良は端のまま。構造が変わらない**
```

**★ 壁の正体が1つの数字になった:**

```
帯に届くまで   CO2 12 step / 水 950 step / O2 2230 step。観測窓は 50
→ 窓の中で拘束されるのは CO2 だけ。制御は ARS 1本、正解は常に「回す」＝定数
判断が生まれるには **同じ窓の中で2つ以上の資源が拘束される**必要がある
理由は窓の短さではなく **ループが閉じていること**。WRS/OGS が回る限り水と O2 は
在庫ではなく平衡である
```

```
EXP-017 f      撃つのが無料             EXP-019 案4  曖昧さが無い
EXP-018 案1    代替に価値が無い          EXP-027 設計層 切りすぎる罰が無い
EXP-028 案3    **拘束される資源が1つしかない** ← これまでの4件の上流
```

**訂正: EXP-024 の「CO2 の警報線↓ → ARS 増 → 水が減る」は原因の帰属が誤り**（経路は乗員の死）。
EXP-024 の「designer 層には代償がある」は、この訂正により支持を失う。

**次にやること（EXP-029 で全面的に差し替え）:**

```
1  案3 を **正しい締め方（O2 × 水）で測り直す**。GPU 0秒・数分
   ただし乗員4では内点の中が櫛状で脆い。「到達できるか」を設計に含める
2  ~~ガードの穴10 を塞ぐ~~ ✅ 完了（`12555e5`）
3  案2（電力の共有）は**未測定**。EXP-018 の却下は散文で、論証が円環していた
4  R4 は「残った唯一の道」ではない。順序は再検討
5  **設計文書 → 監査 → 判断 → 実装 → 記録。EXP-028 はこの順序を破って push した**
```

**（以下は撤回済み・当時の「次にやること」）**
1. ~~**残った道は R4（リーク＋対処手段）だけ。** 案3 は EXP-028 で落ちた。~~
   **設計入力が実測で出ている:**
   ```
   50 step で水を帯に届かせる sink   43.2 L/day ＝ 乗員4の水スループット合計の 4.7倍
   50 step で O2 を致死帯に届かせる  19.5 kg/day ＝ 乗員4の O2 需要の 5.8倍
   既存パラメータは全て乗員代謝に紐づくので掃引では到達不能
   （質量保存の不変条件が実際に90本を拒否した）
   → **リークは定数の値ではなく、代謝に紐づかない新しい項。「平衡を崩す」変更**
   ```
2. **R4 の設計文書を書く前に、原則4 の独立監査を通す。** EXP-028 自身も未監査。
   789 run は決定的で数分で再生成でき、**監査は安い**
3. ルール designer の刻み ×0.9 の取りこぼし（27本）は、R3 に投げる材料として記録済み

**新しい規律（EXP-026 の一段手前）: 設計文書を書く前に、任務が定数で解けないかを見る。**
EXP-025 は2版とも、**任務が定数で解けることを確かめずに**書かれていた。
確かめるのに要ったのは `ea evaluate` 約200本、**GPU 0秒、数分**。

**旧「次にやること」（EXP-026 直後の計画・完了または不要）:**
1. ~~計装／指標／入力の修正~~ → 完了
2. ~~集合A∪B／プラセボ腕／降りる条件／段階1のゲート~~ → **EXP-027 で不要になった**

**旧「次にやること」（第2版の計画）:**
1. ~~LLM designer を測る。基準線が取れたので問いが定まった:~~
   ①無効なレバーを避けられるか（ルールは 309件中 190件が無駄撃ち）
   ②効くレバーを適切な量で引けるか
   ③故障が無い run でも乗員を救えるか（ルールは救えた10本が全て故障注入下）
   **GPU を使う。設計文書 → 監査 → 判断 → 測定の順を守ること**
2. ①（計器）の未決事項は無い。任意で残るのは参考併記案と
   `simulation.initial_o2_storage_kg` / `plant_sim.crew.*` を拒否側へ移す件
2. 案3（水のために運用点を締める）＝世代 v6・全 run 比較不能
3. 「145 mmHg と 127 mmHg のどちらを余裕と呼ぶか」は **R3・チーム判断待ち**（両方に同じ出典）
**「構造的に残る限界」と書いた2件は、限界ではなかった（自己訂正）:**
- **遡っての検証はできる** — `code_version.commit` から当時の scenario.yaml を git で復元し、
  同じ差分を今かける。`~/ea-runs/analysis-tools/retro_bar.py`。全体で 505 クリーン／
  114 のみ復元不能（`code_version` より前の世代）／16 は dirty で参考値。
  **公表 v5 は 24/26 が復元して検証・クリーン**
- `ea evaluate` も baseline の名乗る容積を既定にするよう揃えた（`1f74e84`）

**新しい規律（EXP-022）: 変更を却下する検定は、現状にも当てて併記する。**
片側にだけ物差しを当てれば、どちらを却下したいかで結論が決まる。
**EXP-016/020 は「動かない軸を動かしたい」動機で誤り、EXP-022 は「動かさなくてよい」動機で誤った。**

**無料のものを先に、高いものを後に、測定は最後に1回。**

**問いが変わる。** 「AI は上手いか」は3世代測って答えが出た（ルール腕が有意に優位・
LLM は no-op と区別できない）。次は「**AI をどう組み込めば上手くなるか**」。
いまの比較は LLM 単独 vs ルール単独の競争で、Flow Engineering が「やるな」と言う形。
そして `--design-mode llm` という合成ループを既に持っているのに、全実験で切っている。
規格に紐づいた軸は **3/3**（CO₂・水・O₂）。`ea score` の "not scored" は 0行になった。
R1〜R4 は LLM を走らせないので、設計判断を間違えても失うのは秒。

### 完了済み

### 1. 定格の不変条件 — ✅ 実装済み（2026-08-27）

[rated_capacity_invariant.md](../ssos_eclss_loop/rated_capacity_invariant.md) に決定と検証結果。

3系統すべてが **1 step の経過時間の中で定格を超えて処理しない**。同一 step 内で合算する。
1000倍の注文も、同一 step 6回発行も、合計 1.00x で止まる。

判断3点の結論:
- **WRS 定格 `wrs_capacity_l_day: 13.5` を新設**（導出値。NASA 確認済みの UPA サイジング流儀から）
- **`ars_operation_seconds: 4800` は残す。** clamp を telemetry に出して黙らせない
- **適用は即時**

波及して直したもの: スコアカード C軸が WRS を見逃していた（上限に 10 L バッチ上限を使っていた）。
`test_a_proposal_that_buys_super_rated_capacity_is_refused` は前提が消えたので新契約に書き直した。

**代償**: 世代 v4 になり、v3 の測定（EXP-009・011・012・013）はすべて取り直し。
**要判断で残っているもの**: `max_feed_l_per_operation: 10.0` が到達不能になった（効かない定数）

### 2. ~~世代 v4 で測り直す~~ ✅ 完了（EXP-014）

ノイズ床は測っていない（v3 の n=18〜26 をそのまま採用し 24本で実施）。結論は上に。

**v4 で新しく分かったこと・次に効くもの**:
- **`request_o2` は腕が自分の首を絞められるレバー。** 全滅4本の共通項。
  スコアカード C軸がこれを判断ミスとして捉えられているか未確認
- **持続性の指標が要る。**「ARS を出した step 数 ÷ 帯に居た step 数」が今回いちばん説明力があった。
  D軸に入る性質のもの
- **二峰性に平均を当てるのが適切か。** 中央値 68.708 と平均 61.487 が 7点離れている

### 3. 酸素をキャビン大気にする（設計文書あり・未実装）

[o2_cabin_atmosphere_scoping.md](../ssos_eclss_loop/o2_cabin_atmosphere_scoping.md)

**単位の付け替えでは済まない。** 388 m³ のキャビンは ppO2 160 mmHg で 108 kg、
現行のタンク全量は 8 kg（キャビンなら 11.9 mmHg = 窒息水準）。2プール模型が要る。
現実的な 108 kg にすると 50 step で 2.2% しか動かず、**今の「タンク枯渇で死ぬ」失敗モードが消える**。
**5つの設計判断**（供給とキャビンの境界、失敗モードを何にするか、観測期間、等）が本体。

## 未解決・保留

- **ARS では緊急度を表現できない（v4 の新しい制約）。** 不変条件により goal が天井で頭打ちになり、
  critical 帯の 1.5倍エスカレーションが無効化された。ARS の挙動は「回す／回さない」の二値で、
  CO₂ が 2.1 kg でも 7.9 kg でも同じ。**v4 が ARS について測れるのはタイミングと頻度だけ**で、
  加減は測れない。OGS・WRS は goal が「量」なので影響なし。
  直す道は「ARS の注文を量に変える」か「定格を上げる」。**v4 の数字を取ってから判断する**
- `max_feed_l_per_operation: 10.0` が到達不能になった（定格 0.1875 L/step の53倍）。効かない定数

- **PR #1（served-model-probe, `4502cfe`）が未マージ。** 内容確認が要る
- `2639bfa`（LLM 使用量カウンタ）未 cherry-pick。無いと `llm_usage` は無言で何も記録しない
- **[V2 6004] は1時間平均だが、コードは瞬時値と比較**（step 1200秒なので3 step 移動平均が正しい）
- **`capacity_bounds` の真の上限が未決**（現在は10倍超のみ不合格）
- **D軸はこのシミュレータでは動かない**（常に満点）。**この記述が正しかった。**
  2026-08-28 に一度「v4 で生きた軸になった」と訂正したが、それが誤りで、
  実体は `rated_step_capacity` が `_SATURATION_REASONS` に無かった取りこぼしだった。
  修正済み（`973d2bb`）。**D が 5.000 から外れたら「新しい制限理由が現れた」警報として読む**
- **C 軸は「判断の質」の指標として使えない。** 発行指令数と相関（v5 r=−0.629）し、
  5点のうち1点は `1 − 拒否数/発行数`＝スキーマ適合度、`headroom = min(1, 定格/要求)` なので
  **過小要求は常に満点**（要求0でも満点）。`C.request_sizing` は操作チェックに降格
- **WRS が `fully_satisfied` / `limited_by` を返さない**ので D で判定できない
- スコアカード主軸（終了時の残存率）は乗員4だと5段階しか取れず、腕の比較に198本必要

## 保存場所

```
run データ      ~/ea-runs/ の7ディレクトリ（54 MB、telemetry 全文つき）
                各ディレクトリの README.md に「どのファイルがどの主張を支えるか」の対応表
バックアップ    ~/ea-runs/analysis-tools/backup.sh
                /Volumes/LOGITEC HD/ea-backups/<日時>/ に1本 46MB。取ったあと復元検証まで自動。
                **run データは git 管理外で、消えたら再現不可（seed 固定でも LLM は再現しない）。**
                Time Machine は未設定（専用ディスクを用意して設定する方針・2026-08-28 時点）
                → **GPU を長時間回したあとは必ず走らせる**
                取得時に3つ警告する: 未コミットの変更／対応表 README の欠落／
                dirty な run（`code_version.dirty`）。どれも実際に2回やった失敗
解析ツール      ~/ea-runs/analysis-tools/
                tstat.py = Welch の t 検定（scipy が無いので自前実装、既知値で検算済み）
                compare.py = 腕の比較。物差しを明示し、共通3軸と C・D を並記（EXP-016）
                  **`scoring_bar_modified` のガード入り。C・D の読み方は上の訂正を見ること**
                  公表値は `run 自身の閾値`＝`score_run(dir, habitat=None)`。**`ea scorecard` では出せない**
                  決定的な腕は n=1 なので Welch ではなく1標本 t 検定
                各実験のバッチスクリプト（sweep / noise / temperature / llm-vs-rule）
経緯            この repo のコミット（`git rev-list --count d5616389..HEAD`）+ EXP-000〜028
会話ログ        ~/.claude/projects/-Users-takimototetsuya-Coup-de-Burst-/*.jsonl
次回プロンプト   ~/ea-runs/next-session-prompt.md
```

**run 成果物は自分を生んだ commit を名乗る**（`summary.json` の `code_version`）。
LLM の接続先も名乗る（`summary.json` の `llm`）。EXP-011 の直前に入れた。

## 手順のルール（この枝で守っていること）

1. **「〜が無い」と言う前に確認する。** fetch していないだけのことがある（EXP-000）
2. **定数をテストに合わせて調整しない。** テストが条件を固定する側に回る
3. **数字には n と不確かさを添える。** 点推定だけを読むと EXP-011 の「必要 n = 7」になる
4. **負の結果も記録する。** EXP-002・EXP-012 は棄却の記録
5. **予測を測定前にコミットする。** 走らせた後に予測を書き換えない（EXP-013 は追記で対応）
6. **物理ゲート不合格の run は採点しない。** スコアカードの必須要求
