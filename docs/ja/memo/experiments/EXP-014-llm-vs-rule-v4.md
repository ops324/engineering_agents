# EXP-014 — 定格の不変条件の下で、AI腕はまだルール腕に負けるか

```yaml
experiment_id: EXP-014
date:          2026-08-27
git_commit:    d676152
branch:        experiment/eclss-evaluation-layer
config:        v4（乗員4・定格の不変条件あり・ルール腕はヒステリシス回復）
               plant_sim / 50 step / inject_failures false / design=none
environment:   vLLM qwen3.5-9b @ 10.10.0.108:8000 / temperature 0.45（EXP-012 の決定）
seed:          101（全 run 同一）
n:             llm 24 本 / labeled_rule_base と none は決定的なので各1本
status:        **pre-registered（予測のみ。LLM 腕は1本も走らせていない）**
```

## なぜもう一度測るのか

EXP-013 の答え（ルール腕が有意に優位、LLM 腕は no-op と区別できない）は**世代 v3 のもの**である。
定格の不変条件を入れたので、装置の挙動が変わった。**v3 の測定はすべて比較不能になった。**

何が変わったか:

| | v3 | v4 |
|---|---|---|
| ARS 1回の除去量 | 定格の 4.00倍（0.25 kg） | 定格 1.00倍（0.0625 kg） |
| 注文（goal）で能力を買えるか | **買える。上限なし** | 買えない |
| critical の 1.5倍エスカレーション | 効く | **無効（定格で頭打ち）** |
| 同一 step に複数回発行 | 累積する | 累積しない |
| ルール腕の WARNING 帯 | 一撃のみ | 回復目標まで毎 step |
| 乗員4の CO₂ 余裕 | 4.33倍 | **1.082倍** |

## 測定済みの定数（決定的な腕。LLM を走らせる前に確定済み）

```
共通3軸（残存50 + A20 + B20 = 90点満点）
  labeled_rule_base   50.0 + 19.991 + 12.208 = 82.199   乗員 4/4   final_health safe
  none                37.5 + 19.166 + 12.000 = 68.666   乗員 3/4   final_health warning
```

参考（v3）: ルール 84.31 / no-op 69.38 / LLM 65.25。**腕の間隔は v3 とほぼ同じ**
（v4 13.53点 対 v3 14.93点）。運用点は厳しくなったが、ルール腕は 4/4 を保っている。

## 予測（**走らせる前に書いた。走らせた後に書き換えない**）

### P1 — 物理ゲートは 24/24 合格し、`capacity_bounds` は一度も発火しない

**いちばん強い予測であり、いちばん落としやすい。** 不変条件が正しければ、腕が何を注文しても
定格を超えられない。EXP-012 では 14本中3本が `capacity_bounds` で不合格だった。
**1本でも発火したら不変条件の実装バグである。** 無料の自己検査になる。

### P2 — LLM 腕の共通3軸平均はルール腕（82.199）を有意に下回る

差は v3（−19.06）以上に広がる。**予測区間: LLM 平均 60〜70点。** p < 0.01。

根拠: ARS が1回あたり 1/4 の力になったので、「動かすが足りない」失敗が v3 より重くなる。
一方で注文の水増しによる救済はもう効かない。

### P3 — LLM 腕は no-op 腕（68.666）と、やはり区別できない

**予測: |差| < 6点、p > 0.05。** EXP-013 と同じ質的結論。

### P4 — 二峰性は残り、下の峰は no-op と**厳密に一致**する

ARS を一度も発行しない run は、no-op と同一の軌道になるので 68.666 ちょうどに着地する。
**予測: 24本中3本以上**がこの値（±0.01）を取る。

### P5 — 乗員残存の平均は 3.0〜3.5 人（ルール腕は 4/4）

### P6 — ゲート不合格による除外は起きないので、**採点できる run は 24/24**

v3 では注文の水増しがゲート不合格を招き、採点対象が減っていた。それが消える。

## 外れ方の候補（あらかじめ）

- **P2 が外れる形**: LLM が「毎 step ARS を出す」戦略に偶然たどり着くと、定格いっぱいで
  回すことになりルール腕とほぼ同じになる。不変条件は**下手な腕を罰するが、
  上手い腕とルール腕の差も消す**（どちらも天井に張り付くため）
- **P4 が外れる形**: ARS を出さなくても OGS/WRS の挙動差で軌道がずれ、厳密一致しない可能性
- **P1 が外れたら実装バグ。** その場合は測定を止めて不変条件を直す

## 測定範囲の制約（EXP-014 で測れないこと）

**ARS について測れるのはタイミングと頻度だけで、深刻さに応じた加減は測れない。**
goal が天井で頭打ちになったため、CO₂ が 2.1 kg でも 7.9 kg でも腕にできることは同じである。
詳細は [rated_capacity_invariant.md](../ssos_eclss_loop/rated_capacity_invariant.md) の「代償」節。

OGS・WRS の goal は「量」なので、能力の範囲内では要求量がそのまま効く。ここには分解能が残る。
