# EXP-024 — 誰も回していなかったループを回す

```yaml
experiment_id: EXP-024
date:          2026-08-29
git_commit:    c14f5f9
branch:        experiment/eclss-evaluation-layer
status:        confirmed（測定済み。**予測3つのうち2つ外れた**）
費用:          決定的な腕のみ。1本 0.37秒・120本で約45秒。**GPU 0秒**
```

## なぜこれをやるか

ROADMAP R6 の判断点 **d（設計提案）は「実装済み・一度も測っていない」** と記録されていた。
調べたところ、実態はもう少し細かい:

```
ループ前半（提案を作る）  design_mode=labeled_rule_base で **120 run 実行済み**
                          120本すべてが design_proposals.json を持つ
ループ後半（提案を検証する）proposal_evaluation.json は **~/ea-runs 全体で 0件**
                          ea evaluate は一度も実データに当てられていない
LLM の designer           design_mode=llm の run は **0件**
```

**つまり「提案を出す」までは動いていて、「その提案が本当に効くか」を一度も確かめていない。**
そして確かめる側は**決定的で無料**である（対照/処置の両腕とも `labeled_rule_base`）。

**原則2（無料 → 物理 → LLM）に従えば、LLM の designer に GPU を払う前にここを埋める。**

## 動作確認（1本・実行済み）

```
ea evaluate <run>    0.37秒
  提案 5件（action_profile 2 / set_parameter 2 / service_config 1）
  note: thresholds.co2_storage_high_kg を動かす提案である旨を表示し、
        **両腕とも baseline の物差しで採点**する
  peak_kg 2.051 → 1.989（−0.0625）  terminal_margin +0.0625
  verdict: improved — 2 improved / 0 worsened
```

**物差しが凍結されていること、バーを動かす提案に注記が付くことを確認した。**

---

## 予測（測定前にコミットする。判定規則も併記）

**この枝は直近4回、予測と判定規則を後から書いて撤回した。今回は測る前に書く。**

```
P1  120本の verdict は improved が支配的（80%以上）
    判定: improved の割合を数える。80% 未満なら P1 は外れ

P2  提案の過半数が thresholds.* を含む（＝バーを動かしに行く）
    判定: changes に thresholds.* を含む run の割合。50% 未満なら P2 は外れ

P3  改善は CO₂ 系の指標に集中し、**乗員残存（crew_remaining_frozen）は
    120本のうち 10本未満でしか動かない**
    判定: crew_remaining_frozen の delta が 0 でない run を数える
```

## 解釈規則（これも先に書く）

**P1 と P3 が同時に成り立ったら、それは「良い結果」ではない。**

```
improved が支配的（P1）＋ 乗員が動かない（P3）
  ⇒ verdict の improved は「余裕が少し増えた」を意味するだけで、
     この枝の主指標（乗員残存 50点）に届いていない
  ⇒ **「設計提案ループは動くが、点になる改善は出していない」と読む**

P2 が成り立ったら、提案の主要な戦略が「警報線を下げる」である
  ⇒ 物理を良くしているのか、鳴りやすくしているだけかを分けて数える必要がある
  ⇒ その場合、次に測るべきは **thresholds を含む提案を除いた場合の verdict 分布**
```

**そして、この測定は LLM の designer の良し悪しを一切語らない。**
**語るのは「ルールの designer が作る提案の質」＝ 比較の基準線である。**
基準線が無いまま LLM を走らせれば、EXP-015 の教訓（無料の基準線を先に置く）を破ることになる。

---

# 結果（2026-08-29・120本コピー → 71本が評価可能・実測 34秒・GPU 0秒）

**49本は正しく拒否された** — 2026-08-25 世代は乗員50・actor4 で、後から入れた不変条件
（`plant_sim.crew.size` と `actor.team.count` のロックステップ）に反するため再実行できない。

## 予測の答え合わせ

```
P1  improved が 80%以上          → ★ 外れ。**53.5%**（improved 38 / mixed 17 / no_effect 16）
P2  過半数が thresholds.* を含む  → 当たり。**84.5%**
P3  乗員が動くのは 10本未満       → ★ 外れ。**ちょうど 10本**。しかも**全て +1（救っている）**
```

**解釈規則（先に書いたもの）は発火しなかった。**
「improved が支配的＋乗員が動かない ⇒ 点にならない」— どちらの条件も成立しなかった。

## ★ 結果1 — 効くレバーは1本しかない

```
提案の種類（延べ）  action_profile 125 / set_parameter 119 / service_config 65
thresholds.* を含む提案  n=60  → improved 38 / mixed 17 / no_effect 5   乗員を救った 10本
thresholds.* を含まない  n=11  → **no_effect 11 / 11**                  乗員を救った 0本
```

**`thresholds.co2_storage_high_kg` を下げること以外、何一つ効いていない。**
`action_profile`（125件）と `service_config`（65件）は**全て無効**である。

これは [EXP-018](EXP-018-scarcity-without-value.md) の「指令の54%は0点」と同じ形が、
**actor の層ではなく designer の層で再現したもの**である。

## ★ 結果2 — 乗員が動いた。この枝で R6 方向の変更が 50点軸を動かしたのは初めて

```
10本すべて  乗員 3/4 → 4/4（+1 人 ＝ 共通3軸で +12.5点）
10本すべて  thresholds.co2_storage_high_kg を下げる提案
10本すべて  EXP-019（故障注入）の run
```

**機構は素直である。** CO₂ の警報線を下げる → ARS が早く発火する →
故障の窓（EXP-019 が測った 3 step の崖）を越えられる。**物理的に筋が通っている。**

## ★ 結果3 — `ea evaluate` の verdict は、スコアカードと重要度が食い違う

**乗員を救った 10本は、全て verdict が `mixed` である。**

```
mixed の悪化内訳（延べ）  water_min_l 13 / terminal_margin_kg 7 / o2_min_kg 5
CO₂ が動いた 55本の平均    CO₂ピーク −0.1242 kg ／ 水の最小 −0.0216 L
                          水が悪化 13本 / 改善 30本
```

**人を1人救っても、水が 0.02 L 減れば `mixed` になる。**
`ea evaluate` は9つの指標を等重みで数えるが、スコアカードでは乗員が 90点中 50点である。
**2つの計器が「何が大事か」で食い違っている。**

**これは観察であって、提案ではない。** この枝は「計器が気に入らない→計器を変える」で
4回撤回している。verdict の重みづけを変えるかどうかは、R3（配点の価値判断）と同じ性質の判断で、
**この EXP では触らない。**

## ★ 結果4 — 代償が現れた

[EXP-017](EXP-017-no-tradeoff.md) は「この運用点には代償が無い」と結論した。
**designer の層には代償がある:**

```
CO₂ の警報線を下げる → ARS が増える → 水が減る（55本中 13本で悪化）
```

**小さいが、ゼロではない。** actor の層では見えなかったものが、設計の層では見える。

## 含意 — LLM designer を測る準備ができた

**基準線が取れた。** ルールの designer は:

```
71本中 38本 improved / 17 mixed / 16 no_effect
効くレバーは thresholds.co2_storage_high_kg ただ1本
10本で乗員を1人救う（全て故障注入下）
無効な提案を 190件（action_profile 125 + service_config 65）出している
```

**LLM designer に問うべき問いが定まる:**
1. **無効なレバーを避けられるか**（ルールは 190/309 件が無駄撃ち）
2. **効くレバーを、より適切な量で引けるか**
3. **故障が無い run でも乗員を救えるか**（ルールは救えた 10本が全て故障注入下）

**これは GPU を払う価値のある問いである。** 基準線が無いまま走らせていたら、
「LLM は improved を出した」という無意味な報告になっていた。

## この測定がしないこと

- LLM designer との比較（基準線を取ってから、別の EXP で）
- 採点式の変更（1文字も変えない）
- 物理の変更（無し。既存 run の再実行のみで、世代は変わらない）

## 手戻り

**ゼロ。** 既存 run から派生する対照実験のみ。出力はスクラッチに置き、`~/ea-runs` を汚さない。
